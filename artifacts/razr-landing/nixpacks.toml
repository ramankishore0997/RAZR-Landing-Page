[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @workspace/razr-landing run build"]

[start]
cmd = "npx serve artifacts/razr-landing/dist/public -l $PORT -s"
